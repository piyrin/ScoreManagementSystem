
# 成绩管理系统

一个基于 C++ 开发的成绩管理系统，支持学生、教师、管理员多角色登录，涵盖成绩录入、查询、统计、用户管理等功能。后端采用 SQLite 数据库，支持命令行和 Web 服务。

## 功能特性
- 学生注册、登录、成绩查询
- 教师注册、登录、成绩录入与管理
- 管理员用户管理、系统配置
- 支持成绩统计、课程管理、学生信息管理
- Web 服务接口，前后端分离

## 技术栈
- C++17
- SQLite3
- CMake 构建
- MinGW (Windows 环境)
- 简易 WebServer 实现

## 快速开始
1. 安装 MinGW、CMake
2. 克隆本项目：
	```bash
	git clone https://github.com/piyrin/ScoreManagementSystem.git
	```
3. 编译项目：
	```bash
	cmake -S . -B build -G "MinGW Makefiles"
	cmake --build build
	```
4. 运行服务：
	# 成绩管理系统

	![编译状态](https://img.shields.io/badge/build-%E6%88%90%E5%8A%9F-brightgreen)
	![语言](https://img.shields.io/badge/Language-C%2B%2B-blue)

	> 一个基于 C++ 与 SQLite 的轻量级成绩管理系统，兼容命令行与 Web 服务，支持学生/教师/管理员三级角色的完整流程。

	## ✨ 应用亮点
	- **多角色管理**：学生、教师、管理员各司其职，权限隔离清晰
	- **完整数据链路**：模型 ➜ DAO ➜ Service ➜ 网络，各层职责明确
	- **内置 WebServer**：结合前端 HTML/JS 模板，提供浏览器访问入口
	- **快速部署**：单个可执行文件 + SQLite 数据库即可启动

	## 🛠 技术栈
	- C++17 + STL
	- SQLite3 (静态编译 `sqlite3.c/.h`)
	- Winsock2 + 自定义 HTTP 封装
	- CMake + MinGW Makefiles

	## 🚀 快速上手
	```bash
	git clone https://github.com/piyrin/ScoreManagementSystem.git
	cd ScoreManagementSystem
	cmake -S . -B build -G "MinGW Makefiles"
	cmake --build build
	./webServer.exe
	```
	打开浏览器访问 `http://127.0.0.1:8080`，根据角色登录（配置见 `conf/server.ini`）。

	💡 **Tip**：`bin/score_db.db` 是预置数据库，可直接替换或重置初始化。

	## 📦 项目结构概览
	```
	├── CMakeLists.txt           # 构建入口
	├── main.cpp                 # 启动逻辑 / 路由入口
	├── src/
	│   ├── config/              # 配置与常量
	│   ├── core/Model/          # 实体模型 (User/Student/Teacher/Score)
	│   ├── dao/                 # 数据访问层 (SQLite 封装)
	│   ├── service/             # 业务逻辑 (Auth/Student/Teacher 等)
	│   ├── network/             # 网络层 (HttpServer/Request/Response)
	│   └── utils/               # 工具类 (日志/MD5/Sqlite)
	├── res/                     # 静态资源 (HTML/CSS/JS)
	├── conf/server.ini          # 服务与数据库配置
	├── bin/score_db.db          # SQLite 数据库文件
	└── README.md                # 项目说明
	```

	## 📘 场景速查
	| 场景 | 核心组件 |
	| --- | --- |
	| 学生查看成绩 | `StudentService`、`ScoreDao`、`res/view/user` 页面 |
	| 教师录入成绩 | `TeacherService`、`CourseDao`、`res/view/teacher` 页面 |
	| 管理员配置与用户管理 | `AuthService`、`UserDao`、`res/view/admin` 页面 |

	## 🤝 贡献指南
	1. 提交 Issue 描述问题或特性需求
	2. Fork + 新分支开发，保持代码风格统一
	3. 提交前执行 `cmake --build build` 验证编译无误
	4. 提交 Pull Request，附加复现步骤与测试说明

	## 📬 联系方式
	欢迎通过 GitHub Issue 与我联系，或者直接在 PR 中留言。
